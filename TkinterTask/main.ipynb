from tkinter import *
from tkinter import Tk
from MyCalculator import MyCalculator
from MyDigitalClock import MyDigitalClock
from MyFileExplorer import MyFileExplorer
from MyWeightConverter import MyWeightConverter
from MyAgeCalculator import MyAgeCalculator
from MyNotepad import MyNotepad
from MyCovid19GUI import MyCovid19GUI

root = Tk()
root.title('Tkinter Task App')
root.geometry("450x500+200+300")
root.configure(bg='lavender')

labelfont = ('ariel', 56, 'bold')
l = Label(root, text='Tkinter Task App', font=("Arial", 20, "italic"), justify=CENTER, bg="lavender")
l.place(x=80, y=10)


def myCalculator():
    root = Toplevel()
    my = MyCalculator(root)


def myFileExplorer():
    root = Toplevel()
    file = MyFileExplorer(root)


def myDigitalClock():
    root = Toplevel()
    calc = MyDigitalClock(root)


def myWeightConverter():
    root = Toplevel()
    wt = MyWeightConverter(root)


def myAgeCalc():
    root = Toplevel()
    wt = MyAgeCalculator(root)


def myNotepad():
    root = Toplevel()
    wt = MyNotepad(root)


def myCovidGUI():
    app = MyCovid19GUI()
    # launch the app
    app.mainloop()


widget = Button(root, text="Digital Clock", bg="lightgrey", fg="black", font=("Arial", 14, "bold"), relief=RAISED, bd=5,
                justify=CENTER, highlightbackground="red", overrelief=GROOVE, activebackground="green",
                activeforeground="blue", command=myDigitalClock).place(x=50, y=60)
widget = Button(root, text="Calculator", bg="lightgrey", fg="black", font=("Arial", 14, "bold"), relief=RAISED, bd=5,
                justify=CENTER, highlightbackground="red", overrelief=GROOVE, activebackground="green",
                activeforeground="blue", command=myCalculator).place(x=200, y=60)
widget = Button(root, text="File Explorer", bg="lightgrey", fg="black", font=("Arial", 14, "bold"), relief=RAISED, bd=5,
                justify=CENTER, highlightbackground="red", overrelief=GROOVE, activebackground="green",
                activeforeground="blue", command=myFileExplorer).place(x=50, y=120)
widget = Button(root, text="Age Calculator", bg="lightgrey", fg="black", font=("Arial", 14, "bold"), relief=RAISED,
                bd=5, justify=CENTER, highlightbackground="red", overrelief=GROOVE, activebackground="green",
                activeforeground="blue", command=myAgeCalc).place(x=200, y=120)
widget = Button(root, text="Weight Converter", bg="lightgrey", fg="black", font=("Arial", 14, "bold"), relief=RAISED,
                bd=5, justify=CENTER, highlightbackground="red", wraplength=100, overrelief=GROOVE,
                activebackground="green", activeforeground="blue", command=myWeightConverter).place(x=50, y=180)
widget = Button(root, text="Notepad", bg="lightgrey", fg="black", font=("Arial", 14, "bold"), relief=RAISED, bd=5,
                justify=CENTER, highlightbackground="red", overrelief=GROOVE, activebackground="green",
                activeforeground="blue", command=myNotepad).place(x=200, y=180)
widget = Button(root, text="Covid19GUI", bg="lightgrey", fg="black", font=("Arial", 14, "bold"), relief=RAISED, bd=5,
                justify=CENTER, highlightbackground="red", overrelief=GROOVE, activebackground="green",
                activeforeground="blue", command=myCovidGUI).place(x=50, y=260)
widget = Button(None, text="QUIT", bg="red2", fg="black", font=("Arial", 14, "bold"), relief=RAISED, bd=5,
                justify=CENTER, highlightbackground="red", overrelief=GROOVE, activebackground="green",
                activeforeground="blue", command=root.destroy).place(x=300, y=350)

root.mainloop()
